function AddCourse() {
  return (
    <div>
      <h1>Add Course</h1>
      <p>This is a placeholder page where you can add new courses later.</p>
      <form>
        <input type="text" placeholder="Course Name" />
        <button type="submit">Add</button>
      </form>
    </div>
  );
}

export default AddCourse;
